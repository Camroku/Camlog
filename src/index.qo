#!/usr/local/bin/qo
include qo;
include webutil;

config.author = "abc";

route = deleteAfter(qo.env("REQUEST_URI"), '?');

if (route == "/")
{
    content = format(read("index.html"));
    webutil.htmlPage(content);
}
else
{
    webutil.notFound();
};
